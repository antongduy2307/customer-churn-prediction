[core]
    remote = minio_remote
['remote "minio_remote"']
    url = s3://churn-data/dvc-store
    endpointurl = http://localhost:9000
    access_key_id = minio
    secret_access_key = minio123
    use_ssl = false
